# やることリスト（棚卸し）

最終更新: 2026-09-13 / 凡例: ✅ 完了 ・ ⬜ 未着手 ・ 🔶 要判断

## A. App Refiner 本体（このリポジトリ）

### A-1. 完了
- ✅ STEP1 対象アプリ管理（カード一覧・追加・編集・削除・「開く」・選択状態）
- ✅ カード内 iframe 縮小プレビュー（遅延読み込み、失敗時「プレビュー不可」でレイアウト維持）
- ✅ STEP2 改修要望入力（クイックプリセット7種の追記、改修の種類4種、優先度3段階、文字数カウンタ、下書き自動保存）
- ✅ STEP3 指示書生成（仕様どおりのフォーマット）・コピー（トースト通知）・コピー時の自動履歴保存
- ✅ STEP4 履歴（新しい順、再コピー、対応済みトグル、絞り込み、個別削除、全削除＋確認ダイアログ）
- ✅ 設定：JSON エクスポート（ダウンロード／コピー）・インポート（マージ／置き換え）・初期化
- ✅ localStorage の全アクセスを try/catch 化、失敗時は警告バナー＋一時保持で継続
- ✅ Playwright スモークテスト 34 項目（`tests/smoke.mjs`）

### A-2. 次にやること
- ⬜ **実機確認**（最優先）: 本番URLを iPhone / Android で開き、
  ①「コピー」がクリップボードに入るか ②iframe プレビューが表示されるか ③ホーム画面追加時の見え方
- ⬜ **プレビュー失敗時の見え方の改善**: 到達できないURLだとブラウザのエラーページが白く表示される場合がある
  （`load` イベントが発火するため「プレビュー不可」に切り替わらない）。
  プレビューの手動リロードボタン、またはプレビューON/OFFトグルを検討。
- ⬜ **登録アプリの初期データ**: 現状 `store-trouble-report` / `my-instant-app` / `app-studio-e2e-test` の3件。
  app-refiner 自身を含めるか、実際の保有アプリに合わせて見直す。

### A-3. あると便利（優先度低）
- 🔶 指示書テンプレートの編集機能（「制約」節を自分で書き換えたい場合）
- 🔶 履歴のキーワード検索・アプリ別の絞り込み
- 🔶 PWA 化（manifest + Service Worker）※単一ファイル構成との兼ね合いを要判断
- 🔶 履歴からワンタップで「同じ要望を再依頼」（入力欄へ復元）
- 🔶 改修の種類ごとに制約文を出し分け（例：リファクタ時は「挙動を変えないこと」を追加）

## B. App Studio 側（別リポジトリ / Cloud Run）

詳細な依頼文は [app-studio-feature-request.md](app-studio-feature-request.md) にあります。

- ⬜ **`publish_custom_html` ツールの追加** — 任意の単一HTMLを直接デプロイできるようにする（今回詰まった根本原因）
- ⬜ **稼働リビジョンの追従** — `app-studio-00034-bgp` は `origin/claude/funny-hopper-e88v68` より5コミット遅れ。
  Secret Manager 方式のまま最新コミットをビルド・デプロイ
- ⬜ **承認通知の不具合調査**（今回スマホで再現） — 承認リクエストの通知が届かず、
  `https://app-studio-840352034351.asia-northeast1.run.app/` でも状況を確認できなかった。
  - 期待する改善: 承認待ち一覧を返すツール（例 `list_pending_approvals`）／承認完了後に結果を返す仕組み／
    approve 画面のスマホ対応
- 🔶 `request_publish` のレスポンスに、生成されるHTMLのプレビューURLまたは先頭数行を含める（中身の取り違え防止）

## C. 確認待ち・未検証

- ⬜ 本PRマージ後、GitHub Pages が新しい `index.html` を配信しているか（反映まで数分かかる場合あり）
- ⬜ GitHub Pages のビルド元ブランチ（`main` / `gh-pages`）とビルド方式の確認
- ⬜ 承認チケット `apr_n-iVjhnEliE` の最終ステータス（デプロイ自体は成功済みのため実害なし）
